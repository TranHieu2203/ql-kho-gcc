# Decision Log — Brief ql-kho-gcc

Audit trail cho mọi quyết định, thay đổi, override trong quá trình tạo brief.

## 2026-05-31 — Khởi tạo workspace

- **Intent:** create (tạo brief mới)
- **Ngôn ngữ:** Tiếng Việt (theo config: communication_language=Vietnamese, document_output_language=Vietnamese)
- **Owner:** HieuTV-QL-Kho
- **Input đã có từ hội thoại trước:**
  - File Excel mẫu: `THEO DÕI TỒN KHO LỐP MD-Final.xlsx` (đã phân tích cấu trúc)
    - 4 sheet: DANH MỤC (8 SP), NHẬP KHO (~2115 dòng), XUẤT KHO (~2114 dòng), NXT (tổng hợp)
    - Cột chính: Mã hàng, Thương hiệu, Kích thước, Mã gai, ĐVT (Bộ/Chiếc), Số lượng, Ghi chú
    - Có khái niệm "Khách đặt" (reservation)
    - Khách hàng ghi tự do trong "Ghi chú" — chưa có Customer master
    - Dữ liệu thực tế có lỗi: ngày trống, `#N/A`, số lượng trống
  - Nền tảng: **Web app**
  - Quy mô: **Nhiều kho, nhiều người dùng** (cần auth + RBAC + chuyển kho)
  - Domain: Lốp xe tải (KOIVI, GASVIDO, sizes 1200R20 / 12R22.5 / 1100R20, patterns KV789H3, KV888, GD639, GA518...)

## 2026-05-31 — Discovery (Fast path) — quyết định người dùng

| Câu hỏi | Trả lời | Hệ quả cho brief |
|---|---|---|
| Mục đích brief | Dự án nội bộ / tự dùng | Right-size: bỏ market sizing, GTM, fundraising tone |
| Phong cách | Fast path | Tôi gộp câu hỏi → viết draft → đánh `[ASSUMPTION]` chỗ tôi suy luận |
| MVP add-ons | Báo cáo + In PDF + Import Excel + Chuyển kho. KHÔNG: Customer master, Reservation, Supplier | Brief Scope chốt theo đây; các item KHÔNG dời sang Out-of-Scope với lý do |
| Tiền bạc | KHÔNG — chỉ quản số lượng | Loại bỏ giá / công nợ khỏi MVP, để vào Vision v3 |
| Quy mô | 1-2 kho, 1-3 user, <100 SKU | Stack có thể nhẹ (SQLite khả thi); không cần cluster |
| Kỹ thuật | Responsive mobile + Offline-capable + Self-host | Đưa thành ràng buộc cứng trong Scope, không phải tùy chọn |

## 2026-05-31 — Audit handling

Mọi quyết định trên đã được capture vào:
- `brief.md` — các quyết định nghiệp vụ (scope MVP, target user, success criteria)
- `addendum.md` — chi tiết Excel schema, data model đề xuất, lý do dời các item khỏi MVP, câu hỏi mở cho PRD/Architecture
- `.decision-log.md` (file này) — bảng tra cứu quyết định

Các giả định `[ASSUMPTION]` trong brief đợi người dùng review:
- Timeline thay thế Excel: 1 tháng sau go-live
- Roles: Admin + Thủ kho
- Tech stack & deploy: chốt ở phase Architecture (brief chỉ nêu định hướng)
- Offline conflict: last-write-wins v1
- Xuất quá tồn: cảnh báo vs chặn cứng (cấu hình được)

## 2026-05-31 — Polish pass (doc_standards)

Chạy 2 subagent song song trên `brief.md`:
- `bmad-editorial-review-structure` → 10 findings (cắt/gộp/move/condense). Đã áp: cắt câu meta "không cần bịa moat", gộp duplication Tóm tắt ↔ Điểm khác biệt, cô đọng Vấn đề (dời chi tiết sheet sang addendum), cô đọng Giả định kỹ thuật (dời chi tiết stack sang phase Architecture), cô đọng Tầm nhìn (dời roadmap v2-v5 sang addendum), bỏ vế so sánh trong Success Criteria #3. KHÔNG áp: re-order section "Đối tượng phục vụ" trước "Giải pháp" — để người dùng quyết.
- `bmad-editorial-review-prose` → 10 findings (jargon Anh-Việt, lủng câu, lặp từ). Đã áp tất cả: realtime → thời gian thực, mental model → mô hình tư duy, atomic → trọn vẹn, configurable → cấu hình, scale → mở rộng, moat → bỏ luôn, "Right-sized" → "Vừa đủ", "chỉ-quản-tồn" → "chỉ quản lý tồn kho", bỏ "đang" thừa, đảo "tốn nhiều thời gian đối soát".

Không chạy polish trên `addendum.md` — đây là tài liệu scratch cho PRD/Architecture phase, không cần editorial pass.

External handoffs: skip (workflow.external_handoffs trống).

## 2026-05-31 — Approved

Người dùng (HieuTV-QL-Kho) đã duyệt brief, chọn bước tiếp theo là chạy thẳng `/bmad-prd`.
- Frontmatter status: draft → ready-for-review → **approved**
- Các `[ASSUMPTION]` và 9 câu hỏi mở chưa được resolve ở brief → carry-over sang PRD discovery.

